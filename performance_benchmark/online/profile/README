# VLM 推理性能 Profiling 工具

本目录提供针对 vLLM 服务端的性能分析（Profiling）脚本，支持两种主流 Profiling 方式：

- **PyTorch Profiler**：兼容 Intel XPU（如 B60）和 NVIDIA GPU，生成 `.pt.trace.json.gz` Trace 文件
- **NVIDIA Nsight Systems**：专用于 CUDA GPU，生成 `.nsys-rep` 报告文件

---

## 默认配置

| 参数 | 值 |
|------|-----|
| 模型 | `Qwen3-VL-4B-Instruct` |
| Tensor Parallelism | 1 |
| dtype | float16 |
| 量化 (nsys 脚本) | fp8 |
| max-model-len | 12768 |
| block-size | 64 |
| 端口 | 8000 |

---

## 使用方法

### 方式一：PyTorch Profiler（Intel XPU / NVIDIA GPU 通用）

**Step 1：启动服务端**

```bash
# 自动检测硬件（XPU 走 Intel 路径，其他走标准 GPU 路径）
bash torch_start_server.sh
```

脚本通过 `VLLM_TORCH_PROFILER_DIR` 环境变量指定 Trace 输出目录（默认写入本目录）。

**Step 2：触发 Profiling 并发送请求**

```bash
bash test_client.sh
```

客户端使用 `--profile` 参数通知服务端在本次请求期间开启 Profiler，采集完成后自动停止。

**Step 3：查看 Trace 结果**

生成的 `.pt.trace.json.gz` 文件可通过以下方式查看：

- **Chrome 浏览器**：打开 `chrome://tracing`，加载解压后的 `.json` 文件
- **TensorBoard**：
  ```bash
  pip install torch-tb-profiler
  tensorboard --logdir /llm/performance_benchmark/online/profile
  ```
- **Perfetto UI**：https://ui.perfetto.dev（直接拖入 `.gz` 文件）

---

### 方式二：NVIDIA Nsight Systems（仅 CUDA GPU）

**前置：安装 Nsight Systems CLI**

如容器内未安装 `nsys`，执行以下命令：

```bash
# 安装到 vllm-nv-container 容器（默认版本 2026.1.1）
bash install_nsight_systems_cli.sh

# 指定容器名称和版本
bash install_nsight_systems_cli.sh vllm-nv-container 2025.5.2
```

**Step 1：启动服务端（nsys 模式）**

```bash
bash nsys_start_nv_server.sh
```

报告输出到 `./my_vllm_report.nsys-rep`，使用 `--capture-range=cudaProfilerApi` 实现精细化范围采集。

**Step 2：触发 Profiling**

```bash
bash test_client.sh
```

**Step 3：查看报告**

```bash
# 在本机安装 Nsight Systems GUI 后打开报告
nsys-ui my_vllm_report.nsys-rep

# 或通过 CLI 导出统计摘要
nsys stats my_vllm_report.nsys-rep
```

---

## Profiler 统计摘要说明

`profile_out_*.txt` 为服务端 `vllm bench serve` 运行后输出的 PyTorch Profiler 表格，各列含义：

| 列名 | 说明 |
|------|------|
| `Self CPU %` | 算子自身 CPU 时间占比 |
| `CPU total %` | 含子算子的 CPU 总时间占比 |
| `Self XPU / Self CUDA` | 算子在 XPU/CUDA 设备上的自身执行时间 |
| `XPU total / CUDA total` | 含子算子的设备总时间 |
| `# of Calls` | 调用次数 |
